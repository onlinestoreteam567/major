// import cl from './index.module.scss';

// const Label = ({ labels }) => {
//   // Don't render anything if labels array is empty or undefined
//   if (!labels || labels.length === 0) {
//     return null;
//   }
//   return (
//     <>
//       {(labels.includes('hit') || labels.includes('new')) && (
//         <div className={cl.labelContainer}>
//           {labels.includes('new') && <div className={cl.new}>NEW</div>}
//           {labels.includes('hit') && <div className={cl.hit}>HIT</div>}
//         </div>
//       )}
//       {labels.includes('percent') && <div className={cl.percent}>%</div>}
//     </>
//   );
// };

// export default Label;
